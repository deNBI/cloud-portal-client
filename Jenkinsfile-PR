node {
    def image
    stage('Clone repository') {
        checkout scm
    }

    stage('build image'){
    
                    image = docker.build("denbicloud/cloud-portal-client")
        }                           
}
