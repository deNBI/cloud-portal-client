- name: Setting up your virtual machine
  hosts: all
  become: yes
  connection: paramiko_ssh
  gather_facts: yes
  vars_files:
  tasks:
    - name: Setting up your virtual machine
      block:
      always:
